---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...props } = Astro.props;
---

<div {...props} class={cn("flex items-center gap-3", className)}>
  <slot />
</div>
