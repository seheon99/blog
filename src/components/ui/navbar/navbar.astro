---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...props } = Astro.props;
---

<nav {...props} class={cn("flex flex-1 items-center gap-4 py-2.5", className)}>
  <slot />
</nav>
