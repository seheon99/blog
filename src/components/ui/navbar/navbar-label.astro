---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...props } = Astro.props;
---

<span {...props} class={cn("truncate", className)}>
  <slot />
</span>
